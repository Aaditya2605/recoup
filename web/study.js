const form=document.querySelector('#study'),error=document.querySelector('#study-error'),params=new URLSearchParams(location.search);
form.onsubmit=async event=>{event.preventDefault();error.textContent='';const button=form.querySelector('button'),payload=Object.fromEntries(new FormData(form));payload.submission_id=params.get('submissionId')||'';payload.terac_submission_id=params.get('teracSubmissionId')||'';payload.task_id=params.get('taskId')||'';button.disabled=true;try{const response=await fetch('/api/study/responses',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),value=await response.json();if(!response.ok)throw Error(value.error||'Submission failed');form.innerHTML='<h2>Thank you.</h2><p>Your feedback was recorded. Return to Terac to complete the task.</p>'}catch(reason){error.textContent=reason.message;button.disabled=false}};
